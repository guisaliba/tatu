services:
  app:
    ports:
      - '${PORT:-8000}:${PORT:-8000}'
    healthcheck:
      test:
        [
          'CMD',
          'wget',
          '--no-verbose',
          '--tries=1',
          '--spider',
          'http://localhost:${PORT:-8000}/',
        ]
      retries: 3
